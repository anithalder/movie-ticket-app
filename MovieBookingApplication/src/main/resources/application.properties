spring.application.name=MovieBookingApplication
server.servlet.session.timeout=30m

# ===============================
# DATABASE CONFIGURATION
# ===============================
# Replace 'movie_booking_db' with your actual database name
spring.datasource.url=jdbc:postgresql://pg-3da5d0b4-anithalder360-10fe.j.aivencloud.com:14024/movie_booking_db
# Replace with your PostgreSQL username (default is usually 'postgres')
spring.datasource.username=avnadmin
# Replace with your PostgreSQL password
spring.datasource.password=AVNS_SuNEckDuH1okFKmfusU
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# JPA / HIBERNATE CONFIGURATION
# ===============================
# 'update' will automatically update the schema when you run the app. 
# Use 'validate' or 'none' in production.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ===============================
# SERVER CONFIGURATION
# ===============================
# Default port is 8080, but you can change it here if needed
#server.port=8080

# ===============================
# LOGGING CONFIGURATION
# ===============================